machine:
  xcode:
    version: 9.0.1
dependencies:
  pre:
    - ./scripts/prepare.sh
  cache_directories:
    - "/usr/local/Cellar"
compile:
  override:
    command: ./scripts/deploy.sh
    no_output_timeout: 30m
test:
  override:
    - node -v

machine:
  xcode:
    version: 9.0.1
dependencies:
  pre:
    command: ./scripts/deploy.sh
    no_output_timeout: 30m
  cache_directories:
    - "/usr/local/Cellar"
compile:
  override:
    command: ./scripts/deploy.sh
    no_output_timeout: 30m
test:
  override:
    - node -v
